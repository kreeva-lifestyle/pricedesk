{
  "name": "pricedesk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp CNAME sw.js manifest.json dist/ && cp -r icons vendor dist/",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  }
}
